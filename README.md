# GAN Training Notebook

This repository contains my GAN training notebook

## Included

- `gan-training .ipynb`
- `outputs/images/gan_training_loss.png`
- `outputs/images/normal_vs_attacks.png`

## Not Included In Git

The following are kept locally for development but are intentionally ignored for GitHub:

- input datasets in `data/`
- generated arrays in `outputs/data/`
- model checkpoints in `outputs/models/`
- original extracted Kaggle folders

## Notes

- This is only my part of the overall project, so the repository is not meant to be a complete final submission.
- The notebook has been adjusted to use local folders such as `data/input/` and `outputs/`.
